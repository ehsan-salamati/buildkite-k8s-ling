
    
steps:
- label: "Just wait"
  command: "echo RANMDOM_ENV=$$SOME_RANDOME_ENV, RANDOM_ENV_VAR=$$RANDOM_ENV_VAR"
  agents:
    queue: kubernetes
    
- label: "Kubectl version :congratulations: :champagne:"
  agents:
    queue: kubernetes
  plugins:
    - kubernetes:
        podSpec:
          containers:
            - image: alpine/k8s:1.27.5
              command: [kubectl]
              args:
                - "version"
                - "--short"
              resources:
                limits:
                  cpu: "100m"
                  memory: "128Mi"
